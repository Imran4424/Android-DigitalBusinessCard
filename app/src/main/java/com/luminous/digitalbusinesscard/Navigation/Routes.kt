package com.luminous.digitalbusinesscard.Navigation

// Declares a Kotlin "object" (a singleton class compared to Swift).
object Routes {
        // A compile-time constant String for the Profile screen route.
        // "const val" means:
        // - value is known at compile time
        // - can be used in annotations and places requiring constants
        const val Profile = "profile"

        // Another compile-time constant route name for the Settings screen.
        const val Settings = "settings"
}